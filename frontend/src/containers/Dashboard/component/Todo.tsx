import TodoList from './TodoList';
import TodoStatic from './TodoStatic';

const Todo = () => {
  return (
    <section className="todo">
      <TodoList />
      <TodoStatic />
    </section>
  );
};

export default Todo;
